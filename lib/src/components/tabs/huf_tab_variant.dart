/// Variante visiva di [HUFTabs].
///
/// [primary]: pill con sfondo scorrevole sull'opzione attiva.
/// [secondary]: linea di track con indicatore spesso (sotto o a sinistra).
enum HUFTabVariant {
  primary,
  secondary,
}
