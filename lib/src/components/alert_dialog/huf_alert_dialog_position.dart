/// Posizione verticale di [HUFAlertDialog] nel viewport quando viene mostrato
/// tramite [hufShowAlertDialog].
enum HUFAlertDialogPosition {
  /// Centrato (default).
  center,

  /// Vicino al bordo superiore.
  top,

  /// Vicino al bordo inferiore.
  bottom,
}
